[Unit]
Description=Tablet Hardness Tester kiosk API (Flask)
After=network.target local-fs.target
Wants=network.target

[Service]
Type=simple
User=rle
Group=rle
Environment=PYTHONUNBUFFERED=1
WorkingDirectory=/opt/kiosk

# Free stale listeners on Flask port before bind (Harmless if nothing holds the port.)
ExecStartPre=/opt/kiosk/scripts/kiosk_pre_start_port.sh

ExecStart=/opt/kiosk/run_hardness_bridge.sh
Restart=always
RestartSec=3

# Logs (rle-owned path; avoids permission issues with /var/log)
StandardOutput=append:/home/rle/kiosk_bridge.log
StandardError=append:/home/rle/kiosk_bridge.log

[Install]
WantedBy=multi-user.target
